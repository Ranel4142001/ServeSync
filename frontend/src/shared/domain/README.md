# Shared Domain Layer

This directory represents the client-side representation of our shared domain models and types.

> [!IMPORTANT]
> The frontend domain layer is a client-side reflection for UX purposes only (e.g. form validation, client-side TypeScript mapping). It is **not** the authoritative source of business rules. The backend domain remains the single, authoritative source of all business logic and security rules.
